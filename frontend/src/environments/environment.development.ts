// Ambiente de DESENVOLVIMENTO (ng serve usa este arquivo).
// O backend local sobe na porta 8080 (Spring Boot / Docker Compose).
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080',
};
